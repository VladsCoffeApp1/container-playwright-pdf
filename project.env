# Runtime configuration for Playwright PDF service
# Loaded by docker-compose and CI/CD pipeline

# Application settings
PDF_LOG_LEVEL=INFO
PDF_REQUEST_TIMEOUT=60

# Cloud Run settings (for reference)
# PORT=8080 (set by Cloud Run automatically)
